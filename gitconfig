[user]
  name = Oliver Kletzmayr
  email = okletzmayr@gmail.com
  signingkey = 5E652A5F

[alias]
  up = "!git remote update -p; git merge --ff-only @{u}"
  lol = log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(red)- %an%C(reset)%C(bold yellow)%d%C(reset)'
  lola = log --graph --abbrev-commit --decorate --date=relative --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(red)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all

[bash]
  showDirtyState = true

[commit]
  gpgSign = true

[core]
  excludesfile = ~/.gitignore
  fsyncObjectFiles = true

[color]
  branch = auto
  diff = auto
  status = auto
[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red bold
  new = green bold
[color "status"]
  added = yellow
  changed = green
  untracked = cyan

[format]
  signoff = true

[grep]
  lineNumber = true

[gpg]
  program = gpg2

[push]
  default = current
